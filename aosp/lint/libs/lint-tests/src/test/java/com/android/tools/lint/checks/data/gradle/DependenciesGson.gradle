apply plugin: 'com.android.application'

android {
    compileSdkVersion 19

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 17
    }
}

dependencies {
    compile 'com.google.code.gson:gson:2.2'
}
