#Script to run midnight commander.

#Basically the function and arrow keys work well in linux TERM
export TERM=linux

#run mc in safe graphics mode.. on a regular screen no -a required.. if in xterm mode
#basically just run mc-real on a regular screen / keyboard combo
mc-real -a

